name: Patch Action Authority Fixed Context

on:
  push:
    branches:
      - competition/operator-core-boundary-v1
    paths:
      - 'scripts/patch_action_authority_fixed_context.py'
      - '.github/workflows/patch-action-authority-fixed-context.yml'

permissions:
  contents: write

concurrency:
  group: patch-action-authority-fixed-context-${{ github.ref }}
  cancel-in-progress: true

jobs:
  patch-action-authority:
    runs-on: self-hosted
    timeout-minutes: 10
    env:
      PATCH_PYTHON: /opt/python/3.11.9/bin/python3.11
    steps:
      - name: Materialize exact patch script
        env:
          GH_READ_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          ROOT="${RUNNER_TEMP}/action-authority-patch-${GITHUB_SHA}"
          ARCHIVE="${ROOT}.tar.gz"
          rm -rf "$ROOT" "$ARCHIVE"
          mkdir -p "$ROOT"
          curl \
            --fail \
            --location \
            --silent \
            --show-error \
            --connect-timeout 20 \
            --max-time 300 \
            -H "Authorization: Bearer ${GH_READ_TOKEN}" \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/tarball/${GITHUB_SHA}" \
            -o "$ARCHIVE"
          tar -xzf "$ARCHIVE" -C "$ROOT" --strip-components=1
          test -f "$ROOT/scripts/patch_action_authority_fixed_context.py"
          printf 'PATCH_ROOT=%s\n' "$ROOT" >> "$GITHUB_ENV"

      - name: Patch action safety runtime through Contents API
        env:
          GH_TOKEN: ${{ github.token }}
          REPOSITORY: ${{ github.repository }}
          TARGET_BRANCH: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          [ -x "$PATCH_PYTHON" ]
          "$PATCH_PYTHON" "$PATCH_ROOT/scripts/patch_action_authority_fixed_context.py"
